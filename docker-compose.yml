version: "3.7"
services:
  db:
    image: "postgres:latest"
    environment:
      PGDATA: /tmp
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: desafio-finderup
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"